<?php $payment = $this->prepareData()->getData('payment');?>
<form id="payment" method="POST">
	<table>
		<tr>
			<th>Manage Payment</th>
			<th><input type="button" value="Save" onclick="ajax.setUrl('<?php echo $this->getUrl('save'); ?>').setMethod('POST').setForm('payment').call()"></th>
		</tr>
		<tr>
			<td>Name</td>
			<td><input type="text" name='payment[name]' value="<?php echo $payment->name; ?>"></td>
		</tr>
		<tr>
			<td>Status</td>
			<td>
				<select name="payment[status]">
					<?php foreach (Ccc::getModel('Payment_Resource')->getStatusOptions() as $key => $value) : ?>
						<option value="<?php echo $key; ?>" <?php echo ($key == $payment->getStatus()) ? 'selected' : ''; ?>><?php echo $value; ?></option>
					<?php endforeach; ?>
				</select>
			</td>
		</tr>
	</table>
</form>